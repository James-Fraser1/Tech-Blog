<article class="post">
  <div class="title">
    <h3>{{title}}</h3>
  </div>
  <div class="meta">
    by {{user.username}} on {{created_at}}
    |
    <a href="/post/{{id}}">{{comments.length}} {{comments.length}}</a>
  </div>
</article>